jobs:
    main:
        image: node:6
        steps:
            - install: npm install
            - test: npm test
            - coverage: ./ci/coverage
            - publish: ./ci/publish
        secrets:
            # Publishing to NPM
            - NPM_TOKEN
            # Pushing tags to Git
            - GIT_KEY
            # Uploading coverage information to coveralls
            - COVERALLS_REPO_TOKEN
